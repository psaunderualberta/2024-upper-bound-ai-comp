from setuptools import setup

setup(name='gym_puddle',
      version='0.0.2', #compatible with the new gymnasium
      install_requires=['gymnasium', 'pygame']  # And any other dependencies we need
)
